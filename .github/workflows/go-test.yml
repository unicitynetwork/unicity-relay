name: Tests

on:
  push:
    branches:
      - main
      - master
  pull_request:
    branches:
      - main
      - master
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true

      - name: Verify formatting
        run: |
          unformatted=$(gofmt -l -s .)
          if [ -n "$unformatted" ]; then
            echo "The following files need gofmt -s:"
            echo "$unformatted"
            exit 1
          fi

      - name: Run tests
        env:
          # Ryuk is the testcontainers reaper. The GHA runner is ephemeral
          # and TestMain already calls Terminate(), so we don't need it,
          # and disabling avoids a class of intermittent CI flakes.
          TESTCONTAINERS_RYUK_DISABLED: "true"
        run: go test ./...

      # The full suite under -race reports data races inside khatru's listener
      # bookkeeping (notifyListeners reads the listener list without taking
      # clientsMutex), which zooid cannot fix, so only zooid's own concurrency
      # tests run with the race detector.
      - name: Run concurrency tests with the race detector
        env:
          TESTCONTAINERS_RYUK_DISABLED: "true"
        run: go test -race -run 'TestAuth_ConcurrentDuplicateAuth$|TestLastAuthedPubkey_ConcurrentWithAuth$|TestInstance_PreventBroadcast$' ./zooid/
