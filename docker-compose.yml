services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: zooid
      POSTGRES_USER: zooid
      POSTGRES_PASSWORD: dev
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
